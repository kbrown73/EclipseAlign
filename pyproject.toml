[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "eclipse-align"
version = "0.1.0"
description = "Align OpenEXR solar eclipse timelapse frames."
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
